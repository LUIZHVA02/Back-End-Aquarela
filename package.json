{
  "scripts": {
      "build": "yarn && npm install prisma --save && npx prisma db pull && npx prisma generate && npm install @prisma/client --save",
      "start": "node app.js",
      "dev": "nodemon app.js"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.20.0",
    "prisma": "^5.19.1"
  },
  "devDependencies": {
      "nodemon": "^3.1.4"
  }
}
